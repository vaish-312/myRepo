@model IEnumerable<AspMvcAssign.Models.Detail>

@{
    ViewBag.Title = "Index";
}

<h2>Home Page Table</h2>
<p>
    <a class="text-light btn btn-dark" href="/Details/Create">Add Details</a>
</p>

   
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
           
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
           
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
    <div class="row">
        <div class="">
            <a class="btn btn-primary" href="/ImportExcel/Index" style="margin-top:50px;">Import Excel</a>

        </div>
        <div class="d-md-flex justify-content-md-end" style="margin-top:5px;">
            <a class="btn btn-primary" href="/infoRecs/Index">User Activity</a>

        </div>
    </div>
